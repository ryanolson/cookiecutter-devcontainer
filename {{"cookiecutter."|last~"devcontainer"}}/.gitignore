
# .user is generated by the devcontainer's 00-on-create.sh script
# it captures your user name, default group name, UID and GID
# and forwards them to the devcontainer's entrypoint so you can be
# reinstantiated as the same user inside the container
# 
# ignore this file when commiting changes to the repo
.user
